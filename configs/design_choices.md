# Per-stage design choices

This is the design record for G07's A2 knowledge-base work. It records what has
been observed and what is selected for the next implementation slice;
it does not turn an experiment, an external service result, or a placeholder
into a project score. The current branch now contains conservative baseline
implementations for ingest, identity preprocessing, projection layout, Tesseract
OCR, chunking, embedding, and FAISS persistence. A full-corpus run, hand-labelled
OCR score, and retrieval evidence are still pending, so code presence is not
represented as completed evidence.

| Stage | Problem statement | Data | Model | Methods | Design | Development | Deployment | MLOps |
|---|---|---|---|---|---|---|---|---|
| 0 Frame | Build a searchable, cited knowledge base from a scanned historical medical reference. The explainability target is 100% page citations, each human-verifiable in under 30 seconds. | R. V. Pierce, *The People's Common Sense Medical Adviser* (1890): 1,034 PDF pages and 354,367 usable words in the A1 record. Source: Internet Archive. | No learned model at this stage. The project axes are home-health historical medical reference, dirty OCR, and explainable. | Keep the image as the source of truth; preserve page IDs and the source hash; do not answer from live web search. | Fixed `Page -> Region -> Chunk -> Answer` contracts. The single-book chapter partition is provisional and must be reconciled with the handbook's document-level split rule before evaluation claims. | `configs/task.yaml`, `data/provenance.md`, `grading_kit/manifest.yaml`, and the loader configuration are the current source records. | The full PDF is fetched by `scripts/get_data.sh`; it is not committed to the public repository. | Record the canonical URL, byte count, SHA-256, configuration, and later corpus version ID. |
| 1 Ingest+Enhance | Convert the source PDF into deterministic page images and leave optional enhancement measurable rather than assumed. | Pierce PDF rendered one page at a time at 300 DPI as RGB JPEG, quality 80. | PyMuPDF is the implemented renderer. Enhancement is explicitly disabled; no denoiser, VAE, or diffusion result is claimed. | Atomic writes; reuse only fresh outputs; encode `doc_id`, DPI, format, and quality in the output path; support bounded smoke runs with `page_limit`. | `load_pages()` and the identity `preprocess.run()` preserve the fixed `Page` contract. A learned enhancement can be added later only with a real comparison. | Real Pierce pages were used for loader/preprocess smoke checks, including cache reuse and setting changes. Generated page images remain ignored. | Run acquisition and rendering locally or on the agreed compute environment; do not ship the 1,034-page image tree. | Keep the source hash and render settings beside reproducible runs; do not treat file timestamps as experiment evidence. |
| 2 Layout | Separate text, headings, figures, and captions so reading order and figure citations are not lost. | Each of the five scored detector runs processed all 1,034 Pierce pages. Evaluation uses the 1,028 pages observed by Chandra's provisional reference, containing 353 Image/Figure/Diagram boxes on 254 pages; p0002, p0003, p0004, p0006, p1031, and p1033 are excluded. | Compared projection and `orphan_ink` heuristics with DocLayout-YOLO (DLY), PP-DocLayoutV3 (PPV3), PP-plus-L, and PicoDet. | Greedy one-to-one matching at IoU >= 0.5 gives agreement to the provisional reference; learned detections use score >= 0.5, while orphan ink has no learned confidence. Orphan: P 0.8667, R 0.8102, F1 0.8375 (210.685 s CPU); DLY: P 0.9228, R 0.7450, F1 0.8245 (1,387.365 s CPU); PPV3: P 0.9412, R 0.8159, F1 0.8741 (1,374.454 s CPU); PP-plus-L: F1 0.8341 (1,232.4 s CPU); PicoDet: F1 0.5789 (314.5 s CPU). | PPV3 has the highest agreement. Orphan ink is the fastest but depends on this PDF's embedded text layer. Select DLY as the compact image-only pretrained runtime: the integrated adapter accepts its 75,324,598-byte local ONNX checkpoint. Projection remains the clean-clone fallback. | Each fresh rerun has one canonical record for all 1,034 pages, including explicit empty detections. `layout.detect()` preserves page IDs and image bounds. These values measure agreement to Chandra's provisional reference, not human layout accuracy; human-labelled GT remains pending. | The clean clone runs projection by default (`layout.mode: projection`). DLY runs only when `layout.mode: doclayout_yolo` and a local `weights_path` are configured. Chandra, PPV3, PP-plus-L, PicoDet, and generated rerun outputs remain external artifact evidence in the Kaggle package, not runtime dependencies or additional runtime claims. | Record mode, checkpoint path, score threshold, and missing-page behavior in config; add hand-labelled layout checks before reporting quality. |
| 3 OCR | Read each region while retaining the word geometry needed to highlight supporting evidence. | Real page images and regions; a hand-corrected OCR sample is still required. | The reproducible starter baseline is Tesseract 5 via `pytesseract`; an optional offline `document_ai_reference` mode reads precomputed Document AI words. Neither is ground truth. | Tesseract OCRs each region; reference mode assigns each source-order word to the smallest containing Region and joins closing punctuation naturally. Report CER/WER/F1 only against hand transcription. | `Reader.transcribe_region()` remains a coherent single-Region view; batch `transcribe()` deduplicates overlapping Regions deterministically. The fixed `Chunk` contract stores reference text but not word boxes/confidence. Missing reference words default to `empty` and support explicit `tesseract` or `error`. | Real Pierce p0074 and p0027 reference passes used the 419,565-word output and Chandra Regions. A full normalized coverage check assigned 417,825 words; 1,740 words on 36 pages fell outside every Chandra Region, so full coverage is not claimed. This is pre-annotation/reference evidence, not a score or label. | Tesseract is a local/runtime dependency. Document AI is never called at runtime; a precomputed output must be explicitly configured. | Record engine/source path, language, region mapping, page sample, labels, and output schema for every comparison. |
| 4 Index | Turn verified OCR into searchable chunks while preserving page citations and visual figure links. | 1,028 pages from Chandra OCR output parsed into 2,088 sliding-window chunks (256 tokens, 32 overlap) and 350 figure references across 252 illustrated pages. | `all-MiniLM-L6-v2` at dimension 384 with L2 normalisation for exact cosine similarity search. | Deterministic 256-token chunks with 32-token overlap; FAISS `IndexFlatIP` persistence with atomic `chunks.jsonl`, `metadata.json`, and `image_index.json`. | `Chunk` records retain document/page provenance (`p0001`..`p1034`). `store.build()` writes artifacts under `data/processed/index`; `store.load()` validates metadata and vector counts. | Chunk, embed, store, and `build_index.sh` implementations are verified. Full index built in 35s on 2,088 chunks. | Index is built locally via `bash scripts/build_index.sh` using `uv run`. Store artifacts (index.faiss, chunks.jsonl) are stored in `data/processed/index/`. | Record corpus version, model revision, dimension (384), chunk policy (256/32), count (2,088), and index type (faiss:flat_ip). |
| 5 Retrieval | A3 scope: retrieve and optionally rerank evidence with an evidence-gated widening of `k`. | A3 evaluation questions and labelled relevance are not ready. | A3 model choice is pending. | Pending; no retrieval score or recall claim. | Must consume the fixed store and expose scores/page IDs. | No implementation claim in this A2 record. | A3 scope. | A3 scope. |
| 6 Agent | A3 scope: decide, act, cite, ground, and abstain. | A3 task pairs are not ready. | A3 LLM choice is pending. | Mandatory re-search behaviour is specified by the handbook, not implemented here. | Keep the fixed loop and hook seams unchanged. | No implementation claim in this A2 record. | A3 scope. | A3 trace scope. |
| 7 RL/RLVR | Bonus scope: learned tool selection and verifiable extraction reward. | No reward labels or training run. | Pending bonus decision. | Not required for this A2 increment. | No claim. | No implementation claim in this A2 record. | A4/bonus scope. | A4/bonus scope. |
| 8 Serving | A4 scope: expose a grounded answer API and UI. | Depends on the completed index and agent. | Pending. | Not part of the A2 knowledge-base increment. | Preserve citations and abstention at the serving boundary. | No implementation claim in this A2 record. | A4 scope. | A4 scope. |
| 9 Eval | A2 must add real OCR and retrieval evidence; broader groundedness, robustness, and governance are later. | Held-out page images and labels are not yet present in the starter. | Metrics are pending real labels and runs. | Never infer accuracy from model confidence, detector agreement, or synthetic fixtures. | Evidence must be reproducible from the committed scripts and notebook. | No A2 score is claimed by this design-only commit. | Run evaluation on the held-out slice without training leakage. | Record sample identity, outputs, metric version, and failures. |

## Evidence boundary

- The complete layout artifact package is available in [Kaggle dataset
  version 3](https://www.kaggle.com/datasets/cruelangelssprint/pierce-1890-figure-and-ocr-outputs).
  Git keeps the code and design record; Kaggle holds the generated JSONL,
  crops, and run logs.

- The development directory contains useful experiments, but its detector outputs,
  DocAI output, Chandra notebooks, model weights, virtual environments, generated
  crops, and credentials are not starter artifacts and are not committed here.
- Detector agreement is a prioritisation signal, not ground-truth accuracy.
- The paid Document OCR result is a precomputed reference/pre-annotation source, not
  ground truth and never a query-time API dependency.
- The A2 form, held-out labels, real notebook outputs, and the end-to-end index
  evidence still need to be completed before an `a2-submit` tag is honest.
